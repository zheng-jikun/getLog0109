＠　getLogger.py 文件为读取log的主要函数内容，输出为logger，
    能够直接在命令行中显示结果，也存为了.log文件，也可以后期cp出去变成txt

＠　logger.py 为实际使用中在目标文件代码里应该添加的部分，调用了getLogger的函数，然后有个print的部分
    （没啥实际意义，我单独写出来存个档以后参考）

＠　test_1.py 为实际函数的测试，给了个简单的print（更复杂的内容还没有测试不知道能不能成功）
    store_data.log 中的内容为我执行了几次test_1.py文件的log，没有问题输出的是INFO类型

＠　实际执行直接python对应文件即可，但是需要在文件中添加logger.py里的内容，还是略微有点麻烦